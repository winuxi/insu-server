<div class="flex items-center gap-4">
    <a href="{{ url('/') }}" class="flex items-center gap-4">
        <img src="{{ darkAppLogo() }}"
            alt="{{ darkAppLogo() }}" width="50" height="50">
    </a>
</div>
